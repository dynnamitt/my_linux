#/bin/bash

# avoid C-s issue in VIM
stty stop ^-

# env

export GOPATH=~/gocode
export PATH=$PATH:~/bin
if [ -z "$JAVA_HOME" ]; then
  export JAVA_HOME="/usr"
fi
export SAXON=~/apps/saxon/saxon9.jar






# only if BASH ... I think
#export CDPATH=$CDPATH:./:~/:../:~/code:$GOPATH/src

# Swap caps lock and escape
#xmodmap  - << _EOF_
#remove Lock = Caps_Lock
#keysym Escape = Caps_Lock
#keysym Caps_Lock = Escape
#add Lock = Caps_Lock
#_EOF_
# share hist between termninals
# shopt -s histappend
# PROMPT_COMMAND='history -n;history -a'


